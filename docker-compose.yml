services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    environment:
      - PORT=4000

  frontend:
    build: ./frontend
    ports:
      - "4000:80"
    depends_on:
      - backend